# Decisions

長期的に残すべき設計判断のみを記録する(ADR-lite)。日々の進捗は`STATUS.md`へ。

## D-001 サブスクリプション課金モデル: 無料枠(月10問)+月額300円・単一プラン

Date: 2026-08-30
Status: Accepted

### Context

Claude API原価がAdMob広告収益を大きく上回る赤字構造(詳細はVault内「ランニングコスト・AdMob広告収益の概算」参照)。アプリ正式リリースにあたり赤字を防ぎたいが、収益最大化が目的ではなく最低限の価格に抑えたい。

### Decision

- 無料枠: 月10問(2026-08-30実施のテスト利用ログ分析に基づく初期仮説)
- 有料プラン: 月額300円・単一プラン・使い放題(上限なし)
- 対象: モバイルアプリのみ(LINE Botは対象外、D-002参照)
- 決済実装: RevenueCat経由(Apple/Google純正IAP自前実装は個人開発には過剰と判断)

### Rejected Alternatives

- Apple/Google純正IAPを自前実装(サーバー間通知検証・鍵管理のコストが高すぎる)
- 自動更新なしの「30日間サポーター券」方式(UXが劣り更新忘れのリスク)
- 無料枠を使い放題のまま広告のみで収益化(既存試算で赤字継続が確定的)

### Consequences

- 無料枠の値(月10問)は小規模・短期間のテストデータに基づく初期仮説であり、正式リリース後に実データで再検証が必要
- Apple/Googleの手数料軽減プログラム(中小企業向け、15%)への申請が価格設計の前提

## D-002 LINE Bot版の廃止、モバイルアプリへの一本化

Date: 2026-08-31(廃止完了: 2026-09-02)
Status: Accepted

### Context

サブスクリプション課金機能はモバイルアプリのみを対象とした。LINE Bot版は別系統・別決済手段が必要になり実装コストが大きく膨らむため対象外とした。

### Decision

LINE Bot版を廃止し、モバイルアプリへ一本化する方針。進め方は「告知無しで即時廃止」とユーザーが最終判断(2026-09-02)。理由: LINE Bot経由の利用状況を追跡するログ・DBテーブルが存在せず実利用者数が不明な点、モバイルアプリが既に機能面で上回っている点、無認証・無制限の同期APIがコスト面のリスクである点を踏まえ、告知による移行期間よりも即時停止を優先した。

### Rejected Alternatives

- LINE Bot版にも別途課金導線を実装する(LINE PayとRevenueCat/ストア課金の二重運用はコストに見合わない)
- LINE Bot版を無料のまま存続させる(コスト負担源として残り続ける、無認証・無制限の`POST /api/ruling`が課金回避の抜け道になりうる)
- 告知1回+移行期間を設けてから停止する(Claude提案、ユーザーが不採用。即時停止を選択)

### Consequences

- **2026-09-02完了**: `src/routes/lineWebhook.ts`・`src/line/`(5ファイル)・同期API`src/routes/ruling.ts`(`POST /api/ruling`)・対応テスト2件を削除。`@line/bot-sdk`依存、`LINE_CHANNEL_SECRET`/`LINE_CHANNEL_ACCESS_TOKEN`環境変数、`webhookRateLimiter`を削除
- ジャッジ機能(ログイン・訂正記録・ジャッジ管理)はモバイルアプリ側API(`/api/login`等)で既に代替済みのため、LINE版の`/login`等コマンド削除による機能喪失は無い
- 残作業: Render本番環境変数からのLINE関連キー削除、LINE Developersコンソールでのチャネルの扱い決定(削除/凍結、緊急性は低い)

## D-003 無料枠カウントは独立カウンタテーブルで管理する(ruling_jobの行数を数えない)

Date: 2026-08-30
Status: Accepted

### Context

当初、無料枠の消費数を`ruling_job`テーブルの当月行数で数える設計だったが、モバイルアプリの既存機能「スレッド削除」がこの行を物理削除するため、ユーザーがスレッドを削除するだけで無料枠が復活してしまう不具合が最終レビューで発覚した(PR #1)。

### Decision

`device_monthly_usage`という削除の影響を受けない増分専用カウンタテーブルを新設し、ジョブ作成時にのみインクリメントする。`ruling_job`の行数は無料枠判定に使わない。

### Rejected Alternatives

- スレッド/ジョブを削除ではなく論理削除(`deleted_at`)にする(既存の削除UXへの影響範囲が広く、今回のスコープを超える)
- `llm_usage`ログから数える(ログの保持期間・粒度が課金カウンタとしての用途を想定した設計になっていない)

### Consequences

- カウンタテーブル新設によりデプロイ時に既存デバイスの当月使用済み分が1回だけリセットされる(最大10問、実害軽微、再現性なし)

## D-004 公認ジャッジ訂正を「公式参考情報」として扱う

Date: 2026-09-02
Status: Accepted

### Context

`README.md`はジャッジ訂正を「非公式な過去の誤答実績」「公式情報の根拠としては使わない」と説明していた。一方、実装は、認証済みの公認ジャッジが記録した訂正を公式総合ルール・公式Q&Aと同等に信頼できる資料としてLLMへ提示し、論点が一致する場合は直接の根拠および`high` confidenceの材料として扱っていた。プロジェクト方針と実装で信頼階層が一致していなかった。

### Decision

公認ジャッジによる訂正記録を、本プロジェクトの裁定生成上の「公式参考情報」として扱う方針に統一する。

- 「公式一次情報」は、株式会社タカラトミーが公開するカード情報・総合ルール・公式Q&A・ルール変更を指す
- 「公式参考情報」は、本アプリで認証済みの公認ジャッジが、実際のBot誤答に対して記録した訂正を指す
- 論点が今回の質問と明確に一致する訂正は、公式一次情報と同等に信頼できる裁定根拠として直接使用でき、強い一致であれば`high` confidenceの材料にできる
- 訂正の出典表示では、公認ジャッジの訂正記録であることを明示し、タカラトミー公開物であるかのようには表示しない
- 一般ユーザーの自己申告や、認証されていない訂正は公式参考情報として扱わない

### Rejected Alternatives

- ジャッジ訂正を非公式の誤答再発防止用情報に限定し、直接の裁定根拠には使わない（ユーザーが、公式参考情報としての統一を選択）
- 公式サイト由来の情報が無い場合は、ジャッジ訂正が存在しても常に回答不能にする（蓄積した公認ジャッジ知見を裁定品質へ活用できない）

### Consequences

- 現行の`generateRuling.ts`・`retrieveEvidence.ts`・`confidence.ts`の信頼階層を採用し、README等の説明をこの方針へ合わせる
- 公認ジャッジの訂正はタカラトミー公開物そのものではないため、「公式一次情報」と「公式参考情報」の用語を区別する
- 誤った訂正の影響が大きくなるため、訂正者の認証・judgeIdの追跡・ジャッジ失効手順を維持する

## D-005 AndroidのdeviceIdはインストール単位IDとして扱う

Date: 2026-09-02
Status: Accepted

### Context

サブスクリプション設計書は、`deviceId`をAndroidのEncryptedSharedPreferences等へ保存すれば、アプリデータ削除・再インストール後も同じIDを維持でき、無料枠リセットを防止できる前提だった。しかしAndroidのローカル保存だけではこの永続性を保証できず、`flutter_secure_storage`の暗号化SharedPreferencesをAuto Backupで復元すると、復元されないKeystore鍵との不整合で読めなくなる可能性もある。また、現在の脅威モデルではAPIへ任意の`deviceId`を直接送る能動的な不正を既知の限界として既に受容している。

### Decision

ログイン不要のUXを維持し、Androidの`deviceId`を永続的な端末・ユーザー識別子ではなく「インストール単位ID」として扱う案Aを採用する。

- アプリデータ削除・アンインストール/再インストール後は、新しい`deviceId`が生成され得ることを仕様上の既知の限界として受容する
- 無料枠は新しいIDに対して再度付与され得る。厳密な防止より、ログイン不要の利便性と小規模アプリとしての実装・運用コストを優先する
- `DeviceIdProvider`は、Secure Storageの読み書きに失敗しても同一プロセス内では同じIDを返し、並行呼出しでも複数IDを生成しないようメモリ/進行中Futureをキャッシュする
- Android Auto BackupをID永続化手段として使わず、`flutter_secure_storage`のSharedPreferencesをバックアップ対象から除外する（必要ならアプリ全体のAuto Backup無効化ではなくbackup rulesで限定除外する）
- 購読復元はdeviceId永続化から分離する。RevenueCatのRestore Behaviorを`Transfer to new App User ID`とし、`restorePurchases()`成功後にバックエンド同期を行って、新しいIDへ購読状態を反映する
- Push通知用インストール識別子、購読主体、将来のユーザーアカウントは概念上分離する。現時点で一般ユーザーアカウントは導入しない

### Rejected Alternatives

- 一般ユーザーへGoogle/Apple等のログインを必須化し、サーバー発行`accountId`で無料枠・購読・履歴を管理する（最も厳密だが、現在のログイン不要設計と個人開発の運用規模に対して負荷が大きい）
- Android Auto BackupやApp Set IDだけで再インストール後の同一IDを保証する（リセット条件があり、永続IDとして保証できない）
- ハードウェア識別子を利用する（Androidのプライバシー方針・権限制約に反し、用途に対して過剰）

### Consequences

- 「再インストール後もdeviceIdを維持し、無料枠リセットを防ぐ」という旧仕様は廃止し、課金仕様書・テスト方針・プライバシー説明を更新する
- 再インストール後、無料枠・質問履歴・Push通知の紐付けは新規扱いになり得る
- 有料購読はストアアカウントとRevenueCatの購入復元機能から回復できるよう、実機E2Eで確認する
- 実際に無料枠リセットの悪用が顕在化した場合は、Play Integrityによるリクエスト検証または一般ユーザーアカウント導入を別タスクとして再検討する

## D-006 個別裁定知識を「検証済み裁定原則」データへ移行する

Date: 2026-09-02
Status: Accepted

### Context

`src/ruling/generateRuling.ts`のシステムプロンプトには、過去の誤答再発防止のため、ルール15〜20として個別裁定の解釈が常時ハードコードされている(コミット`5cc0235`のキリコ³の件、コミット`1eea261`のドギラゴン逆・ブルーインパルスの件が契機)。回答安定化には効果があったが、(1) 質問と無関係でも毎回全文が注入される、(2) 出典・確認日・適用除外条件を構造化して管理できない、(3) 公式ルール変更に追随しにくい、(4) 検索評価と回答正当性のテストが分離されていない、という問題がある。実測でルール15〜20はシステムプロンプト全体(約10,734文字)の約46%(4,901文字)を占めるが、過去のクイズ検証(計100問)で実際に関与したのは1割未満と見られ、大半のリクエストで無関係な情報が送信されている。Codex横断レビュー(`.ai/reviews/2026-09-02-ruling-knowledge-migration-proposal.md`)でこの移行案が提案された。

### Decision

個別裁定の常時ハードコードを廃止し、出典・適用条件・確認日を持つ「検証済み裁定原則」データへ段階的に移行する。

- 正本はビルド対象の`src/rules/data/verified-ruling-principles.json`(Git管理)とし、各原則は`id`/`title`/`ruling`/`appliesWhen`/`doesNotApplyWhen`/`officialRuleIds`/`officialQaUrls`/`triggerKeywords`/`verification`/`verifiedAt`/`status`を持つ。Renderの永続ディスクは`/app/data`にマウントされる実行時データ用のため、Git管理された静的な正本データはそこに置かず、ビルド成果物に同梱する
- 質問のルール概念・キーワードから関連する原則だけを検索・取得し、`RulingEvidence`の新しいセクションとしてLLMへ注入する(常時ではなく該当時のみ)
- 取得した原則は、`doesNotApplyWhen`に該当しないことをLLMに必ず確認させたうえで、論点が一致すれば直接の裁定根拠として使用できる。ただし過去の訂正事例(D-004)が人間(公認ジャッジ)による個別確認済みの強い一致であるのに対し、検証済み裁定原則はトリガーキーワードの機械的一致で取得されるため、機械的なconfidence推定(`confidence.ts`)では他のEvidence種別と同様に安全側の判定に留め、highへの自動昇格対象にはしない
- 一括削除せず、提案書のMigration Order(比較的移行しやすいルール17・18・19→要再確認のルール15・16→公式根拠特定が必要なルール20)に沿って1件ずつ移行し、新方式で旧方式と同等以上の結果を確認してから対応するシステムプロンプト記述を削除する
- 各原則の検証記録(`verification`)にはD-004の「公式一次情報」「公式参考情報」の区別を反映する

### Rejected Alternatives

- 現状のハードコードを維持する(コスト・保守性の問題を放置することになり、提案書の指摘を無視する判断のため不採用)
- ルール15〜20を検証なしで一括削除し従来のRAG検索のみに戻す(過去に発覚した誤答の再発リスクがあるため不採用)
- 検証済み裁定原則のヒットを機械的confidence推定でhigh候補に含める(トリガーキーワードの単純一致だけでは誤ヒットを判別できず、過剰マッチ時に誤った断定を助長するリスクがあるため不採用。LLM自身の論点一致判断と自己評価に委ねる)

### Consequences

- `retrieveEvidence.ts`・`generateRuling.ts`・`RulingEvidence`型に新しいEvidence種別が追加される
- 移行完了までは新旧2方式が一時併用され、移行済みルールとハードコード継続中のルールが混在する期間が生じる
- 各原則の検索網羅性(正例・負例の評価ケース)を継続的にメンテナンスする運用コストが発生する
- トリガーキーワードの単純一致による検索は、doesNotApplyWhenに該当する質問にも原則が過剰に注入されうる。最終的な適用可否の判断はLLMの指示(システムプロンプト該当ルール)に委ねる設計とする
